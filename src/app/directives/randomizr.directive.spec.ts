import { RandomizrDirective } from './randomizr.directive';

describe('RandomizrDirective', () => {
  it('should create an instance', () => {
    const directive = new RandomizrDirective();
    expect(directive).toBeTruthy();
  });
});
